# Database Configuration
# Railway MySQL connection using public network domain
spring.datasource.url=jdbc:mysql://forest-management.up.railway.app:3306/railway?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=hFLmjcGhRpKrElXiwBNAJLINcSfrfzqu
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
# Use PORT environment variable for cloud deployment, default to 8080 locally
server.port=${PORT:8080}

# Application Name
spring.application.name=Forest Management System

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.forest.management=DEBUG
