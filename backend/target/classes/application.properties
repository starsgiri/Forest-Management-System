# Database Configuration
# Use Railway's internal network variables (available within Railway services)
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:forest_management_system}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:hFLmjcGhRpKrElXiwBNAJLINcSfrfzqu}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
# Use PORT environment variable for cloud deployment, default to 8080 locally
server.port=${PORT:8080}

# Application Name
spring.application.name=Forest Management System

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.forest.management=DEBUG
